{
  "name": "backend",
  "version": "0.0.1",
  "description": "the backend for an app that tracks learinng",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc && node dist/app.js",
    "dev": "npx tsc && node dist/app.js",
    "build": "npx tsc"
  },
  "type": "module",
  "author": "Justus bruegmann",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "postgres": "^3.4.9",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
